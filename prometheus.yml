global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'veridex-backend'
    static_configs:
      # Use host.docker.internal to scrape FastAPI running on the host machine
      - targets: ['host.docker.internal:8000']
